% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blendsurv.R
\name{blendsurv}
\alias{blendsurv}
\title{Blended survival object}
\usage{
blendsurv(
  obs_Surv,
  ext_Surv,
  blend_interv,
  beta_params = list(alpha = 3, beta = 3),
  times = NULL,
  nsim = 100
)
}
\arguments{
\item{obs_Surv, ext_Surv}{Observed and external data survival curves.
These can come from \pkg{survHE}, \pkg{INLA} or \pkg{flexsurv} fits.}

\item{blend_interv}{A named list containing \code{min} and \code{max} values
defining the blending interval (e.g., \code{list(min = 48, max = 150)}).}

\item{beta_params}{A named list containing the shape parameters \code{alpha}
and \code{beta} of a beta distribution (e.g., \code{list(alpha = 3, beta = 3)}).}

\item{times}{A vector of times for which the survival curves
are to be computed; optional.}

\item{nsim}{The number of simulations drawn from the posterior parameter distributions of
the survival models to propagate uncertainty; default is 100. If \code{nsim = 1},
only point estimates are generated without credible intervals.}
}
\value{
An object of class \code{blended}, which is a list containing the following elements:
\itemize{
\item \code{S}: A data frame with the point estimates of the blended survival curve. If \code{nsim > 1}, it also includes the lower (\code{low}) and upper (\code{upp}) 95\% credible intervals.
\item \code{sim}: Currently \code{NA}.
\item \code{nsim}: The number of simulations used.
\item \code{mat}: A matrix of the simulated blended survival probabilities (rows represent time points, columns represent simulations).
\item \code{des.mat}: Currently \code{NA}.
\item \code{times}: A numeric vector of the time points at which the survival curves were evaluated.
\item \code{S_ext}: A matrix of the simulated external survival probabilities.
\item \code{S_obs}: A matrix of the simulated observed survival probabilities.
\item \code{weight}: A numeric vector of the weights applied at each time point, derived from the beta distribution.
\item \code{blend_interv}: The blending interval used.
\item \code{beta_params}: The parameters of the beta distribution used for blending.
}
}
\description{
This is the main function in the \pkg{blendR} package.
Two survival curves are supplied and blended according to
the blending distribution characterised by the blending interval and
the beta distribution parameters.
}
\details{
The \code{obs_Surv} and \code{ext_Surv} arguments accept several types of fitted
survival models and data structures. Exhaustively, the accepted objects are:
\itemize{
\item \code{survHE} objects: Fitted survival models generated by the \code{fit.models()} function from the \pkg{survHE} package.
\item \code{inla} objects: Bayesian survival models fitted using the \pkg{INLA} package, typically generated via \code{INLA::inla()} or the helper function \code{fit_inla_pw()}.
\item \code{flexsurvreg} objects: Parametric survival models generated by \code{flexsurvreg()} or \code{flexsurvspline()} from the \pkg{flexsurv} package.
\item \code{matrix} objects: A numeric matrix of pre-calculated survival probabilities, where rows correspond to discrete time points and columns correspond to simulations.
\item \code{numeric} vectors: A single numeric vector of pre-calculated survival probabilities (e.g., extracted from a Kaplan-Meier estimate like \code{survival::survfit()$surv}).
}

These objects are processed internally using the \code{make_surv()} S3 generic to ensure they are converted into a standardized format.

Simulation is used to propagate uncertainty from the underlying survival models to the final blended curve.
Specifically, \code{nsim} survival curves are simulated from the posterior parameter distributions of both the
observed and external models. These simulated curves are then blended pairwise at each iteration. This approach
produces a distribution of blended survival curves, which allows for the calculation of credible intervals.
}
\examples{
\dontshow{if (rlang::is_installed("survHEhmc")) withAutoprint(\{ # examplesIf}

library(survHE)

## trial data
data("dat_FCR", package = "blendR")

## externally estimated data
data_sim <- ext_surv_sim(t_info = 144,
                         S_info = 0.05,
                         T_max = 180)
# observed survival times
obs_Surv <- fit.models(formula = Surv(death_t, death) ~ 1,
                       data = dat_FCR,
                       distr = "exponential",
                       method = "hmc")

# external survival times
ext_Surv <- fit.models(formula = Surv(time, event) ~ 1,
                       data = data_sim,
                       distr = "exponential",
                       method = "hmc")

# blending parameter values
blend_interv <- list(min = 48, max = 150)
beta_params <- list(alpha = 3, beta = 3)

ble_Surv <- blendsurv(obs_Surv, ext_Surv, blend_interv, beta_params)

# all survival curves
plot(ble_Surv)
\dontshow{\}) # examplesIf}
}
\references{
Che, Z., Green, N., & Baio, G. (2022). Blended Survival Curves: A New Approach
to Extrapolation for Time-to-Event Outcomes from Clinical Trials in Health
Technology Assessment. Medical Decision Making, 43(3), 299-310.
\url{doi:10.1177/0272989X221134545}.
}
\seealso{
\code{\link[survHE:fit.models]{survHE::fit.models()}}, \code{\link[INLA:inla]{INLA::inla()}}, \code{\link[flexsurv:flexsurvreg]{flexsurv::flexsurvreg()}},
\code{\link[flexsurv:flexsurvspline]{flexsurv::flexsurvspline()}}, \code{\link[=make_surv]{make_surv()}}
}
